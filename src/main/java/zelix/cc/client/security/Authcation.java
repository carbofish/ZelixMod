package zelix.cc.client.security;

public class Authcation {
    public static Authcation authcation = new Authcation();
    public void authClient(int UUID) throws ClassNotFoundException {
        if((UUID + "").length() < 2){
            return;
        }

    }
}
